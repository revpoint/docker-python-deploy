FROM docker-python-deploy-base

ADD wheelhouse /wheelhouse
RUN . /appenv/bin/activate; \
    pip install --no-index -f wheelhouse DockerApp

EXPOSE 8081

ENTRYPOINT . /appenv/bin/activate; run-the-app
